# Copy to secrets.env for onboarding — do not commit real values.
DATABASE_URL=postgresql://voxa:REPLACE@pgbouncer.data.svc.cluster.local:6432/voxa
REDIS_URL=redis://redis.data.svc.cluster.local:6379/0
# When REDIS_URL is set, the API fans out WebSocket sync events across replicas.
