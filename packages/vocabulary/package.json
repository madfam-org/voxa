{
  "name": "@voxa/vocabulary",
  "version": "0.1.0",
  "private": true,
  "description": "Fitzgerald Key, GLP helpers, and motor-planning validators",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --import tsx --test src/index.test.ts src/grid.test.ts",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@voxa/core": "workspace:*"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
