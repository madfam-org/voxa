{
  "name": "@voxa/access",
  "version": "0.1.0",
  "private": true,
  "description": "Switch scanning and eye-tracking access adapters",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --import tsx --test src/index.test.ts src/scan-audio.test.ts src/touch-guard.test.ts",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
