{
  "name": "@voxa/sync",
  "version": "0.2.0",
  "private": true,
  "description": "Cloud sync client for Voxa boards — REST + WebSocket",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@voxa/core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
