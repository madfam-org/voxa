# Environment variables (copy to .env.local)

# API
PORT=4000

# Web
# Production API (mobile release builds, EAS secrets, etc.)
# NEXT_PUBLIC_API_URL=https://voxa-api.madfam.io
NEXT_PUBLIC_API_URL=http://localhost:4000
